import { AccountTypeCodePipe } from './account-type-code.pipe';

describe('AccountTypeCodePipe', () => {
  it('create an instance', () => {
    const pipe = new AccountTypeCodePipe();
    expect(pipe).toBeTruthy();
  });
});
